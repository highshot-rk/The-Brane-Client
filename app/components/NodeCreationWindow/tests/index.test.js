// import NodeCreationWindow from '../index'

import expect from 'expect'

describe('<NodeCreationWindow />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true)
  })
})
