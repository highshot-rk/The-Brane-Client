// import { selectPropertyFiltersDomain } from '../selectors'
// import { fromJS } from 'immutable'
import expect from 'expect'

// const selector = selectPropertyFiltersDomain()

describe('selectPropertyFiltersDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true)
  })
})
